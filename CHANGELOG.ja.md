# OpenTerminal 更新履歴

## v1.0.15 - 2026-09-20

- **ターミナルの背景画像をカスタマイズ**：設定 → テーマ → 背景画像で、任意のローカル画像をターミナルの背景に指定でき、画像の不透明度（10%～100%）も調整可能。画像を削除すると単色背景に戻ります
- **テーマエディターの名前重複警告**：新規作成・編集時に内蔵テーマや他のテーマと名前が重複すると黄色のヒントを表示（保存は可能）
- テーマエディターのシードカラーを自動正規化し、無効なカラー値がカスタムテーマに入らないように
- 破損したカスタムテーマのカラーはデフォルト色に自動修復され、警告ログを記録

- 設定ダイアログ左側ナビの選択中項目の文字を白色に変更し、未選択項目と同じコントラストに（濃い背景で青い文字が見えにくい問題を修正）
- 背景画像・不透明度などの新規コントロールと4言語（簡体字/繁体字/英語/日本語）の文言を同時リリース

- レンダラーとメインプロセスにフリーズ時間の検出ログを追加し、「応答なし」問題の今後の診断に役立てます

## v1.0.14 - 2026-09-20

- **約 250KB を超える**アップロードファイルが静かに破損する問題を修正（サイズは正しいのに内容のバイトが乱れる）
- ダウンロードのたびにリモートのファイルハンドルが漏出し、転送のたびに SFTP チャネルが残る問題を修正。サーバーのプロトコル異常でアプリ全体が落ちることもなくなりました
- 同名ファイルへのアップロードは上書き確認を表示。複数ファイル削除の部分失敗も正しく報告されます
- 権限エディタの修正：不明なモードのファイルに誤って `chmod 000` を送らない。空の GID が新しい UID で置き換えられない。setuid / setgid / sticky ビットを表示・保持
- chmod / chown にタイムアウトを追加し、遠端の無応答で永久にスピンしなくなりました

- SSH 出力の日本語・中文が文字化けする問題を修正（ネットワークパケットをまたいで分割されたマルチバイト文字）
- Tab 補完を確定した後にコマンド履歴が失われる・壊れる問題を修正
- Ctrl+U / Ctrl+W などの編集キーがコマンド履歴に書き込まれる問題を修正（「2 つのコマンドが連結される」症状）
- `cd ~/ディレクトリ` や `cd $HOME/...` が正しく記憶され、新しいターミナルで復元されます
- CJK 文字を含む行内のリンクのクリック範囲がずれない。極端に小さいサイズをターミナルに送らない
- SSH セッションは実際の終了コードを表示。ZMODEM のタイムアウト・エラーでターミナルやログに文字化けを流し込まない

- セッション復元に失敗しても、その実行中のスナップショット保存が止まらなくなりました
- モニターの CPU / ネットワーク履歴グラフがリアルタイムに再描画されます。SSH ワークスペース非表示中はポーリングを停止
- ブロードキャスト：SSH 分割の片方を閉じてももう片方がブロードキャスト対象に残ります。チェックを外しても他の選択をクリアしない
- 接続失敗時に実際の理由を表示。ダブルクリックで重複接続しない
- 「他を閉じる / 右側を閉じる」がタブを飛ばして閉じる問題を修正
- 言語切り替えが最初のフレームから有効。テーマ切り替え後にアクティブタブの文字色が自動で調整され、明るいテーマでも読める
- 秘密鍵パス欄の説明を「この PC 上のパス」に修正（以前はサーバー上のパスと表示され、その通り入力すると接続に失敗していました）

- ウィンドウへのファイル ドロップによるナビゲーションを禁止。外部リンクは http(s) / ftp のみ。IPC に送信元チェックを追加
- 保存パスワードはパスワード認証のブックマークにのみ使用（鍵認証のブックマークがパスワードをフォールバック送信しない）
- 接続ブックマークやホスト指紋をアトミックに書き込み、クラッシュでデータが消えない
- レンダラープロセスの異常終了時に、そのセッションとログを自動回収
- 破損したカスタムテーマでアプリが起動しなくなる問題を修正
- 更新チャネルの公開をアトミックに。公開中の失敗で自動更新が使えなくなることはなくなりました

## v1.0.13 - 2026-09-20

- 簡体字中国語 / 繁体字中国語 / English / 日本語 に対応。設定 → システム → 表示言語 から切り替え、即時反映され保存されます
- アプリ全体を翻訳：設定の各ページ、ワークスペース、ターミナルの右クリックメニューと検索、SSH 接続管理、ファイルパネル、システムモニター、コマンドパネル、トレイメニューと終了確認
- antd コンポーネントの内蔵文言（空状態・ページネーションなど）も表示言語に追従

- 更新履歴は**アプリに同梱**され、オフラインでも表示できます（ネットワーク不要）
- 更新履歴も多言語対応：現在の表示言語で表示し、未翻訳のバージョンは簡体字中国語にフォールバック

- 「閉じるボタンの動作 = 毎回確認」が機能しない問題を修正（従来は「トレイに最小化」へ無言で書き換えられていました）
- ハイライトルールが形式の問題で無言で破棄されなくなりました：優先度の文字列、0/1 の真偽値、前景色の欠落などは自動修復し、未知のフィールドは保持します
- 設定の解析警告を `settings-warnings.log` に記録し、設定異常を追跡可能にしました

- ターミナル右上のツールバーを設定可能に（設定 → 描画と動作）：作業ディレクトリを開く（既定で有効）、セッションログの記録、ログフォルダを開く
- 「作業ディレクトリを開く」：ターミナルの現在のフォルダをエクスプローラーで開きます（`cd` に追従）
- グローバル表示ショートカットを**キー録音式**に変更：入力欄をクリックして組み合わせキーを押すだけ。Esc でキャンセル、Backspace で消去。修飾キーまたはファンクションキーが必要で、通常の文字入力を奪いません
- 「入力候補」「コマンド履歴の記録」は既定でオフ（既存インストールは一度だけリセットされ、以後の手動設定は上書きされません）
- 設定ダイアログの内容欄がスクロールしない問題を修正（antd v6 のコンテナクラス名変更による）。テーマページの二重スクロールバーも解消
- UI 調整：描画モードと言語のボタンを連結セグメント化し、選択時の文字コントラストを修正

リリース時に `scripts/sync-changelog.cjs` が `RELEASE_NOTES.ja.md` をこのファイルへ
マージします。内容はアプリに同梱され、「情報」タブは現在の表示言語の更新履歴を
表示します（未翻訳のバージョンは簡体字中国語にフォールバック）。

## v1.0.12 - 2026-09-20

### ライトテーマのコントラスト修正
- タブバーとサイドバーの色をテーマ背景の明度から算出するように変更：ライトテーマではタブバーがターミナルと同じ色味になり（濁ったグレーの帯が解消）、ダークテーマの見た目は変わりません
- ライトテーマでタブが濃いブロック＋白文字になっていた問題を修正：タブの背景と文字をテーマ変数から取得し、非アクティブタブは淡色背景＋濃い文字、アクティブタブの文字は自動的に濃くなります
- 設定ダイアログ全体をライトテーマに対応：フォント／ハイライト／テーマ各ページのラベル・説明・表・パレットの文字がテーマの前景色に追従し、明るい背景でも読みやすくなりました

### 安定性
- セッションログの書き込みをファイル単位のバッファ + 単一の書き込みループに変更：大量出力時はまとめて書き込まれ、順序も保証され、書き込み回数に応じて無限に溜まることはなくなりました
- 設定の保存を直列化：トレイからの終了動作の変更と設定画面からの保存が同時でも上書きして更新を失わなくなりました
